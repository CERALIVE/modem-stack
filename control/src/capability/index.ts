// Capability-module feature-gate framework — the support-claim taxonomy and the
// per-modem capability detection the seven gated modules resolve against.
//
// Framework only: no module is implemented here, and none may be surfaced or
// claimed until its own change lands with its probe and its evidence.

export * from './detect';
export * from './support-claim';
